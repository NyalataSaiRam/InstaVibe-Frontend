import { BlurImageDirective } from './blur-image.directive';

describe('BlurImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurImageDirective();
    expect(directive).toBeTruthy();
  });
});
